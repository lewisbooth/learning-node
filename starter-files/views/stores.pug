extends layout

include mixins/_storeCard
include mixins/_pagination

block content
  .inner
    h2= title
    .stores
      if stores.length > 0
        each store in stores
          +storeCard(store)
      else 
        p No stores found! 
          a(href="/add") Add one here
    +pagination(page, pages, count)